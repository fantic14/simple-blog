# Simple Blog Application

The README file for the backend is located in the [blog-app](./blog-app) directory, and the README for the frontend is in the [blog-frontend](./blog-frontend) directory.

To build and run the application using Docker, execute the following command from the root folder:

```bash
docker compose up --build
```
